# Integration Options
- **Brokers**: Dhan API, Zerodha Kite. Use adapter pattern to support both.
- **Data Feeds**: NSE Live (current), Yahoo Finance (fallback), specific institutional APIs if available.
- **AI/LLMs**: Ollama (local), OpenAI (cloud fallback).
- **Notifications**: Telegram (current), Slack/Email (future).
